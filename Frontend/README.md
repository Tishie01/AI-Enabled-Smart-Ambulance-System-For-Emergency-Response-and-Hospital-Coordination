# Ambulance Frontend (starter)

This is a Vite + React starter using Tailwind (placeholder for v4). After installing dependencies, run `npm run dev`.

Quick start:

```bash
cd Frontend
npm install
npm run dev
```

Use the simple UI to start a session from the ambulance side and test guardian flow.
